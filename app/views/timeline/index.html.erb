<h1>Listing events</h1>

<table class="table">
  <thead>
    <tr>
      <th>Date</th>
      <th>Event</th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
	<td><%= event.date %></td>
        <td><%= event.event %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
