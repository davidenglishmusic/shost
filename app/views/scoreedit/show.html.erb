<%= tag.div id: 'score-edit-current-page-information',
            data: {
              notes_default_song_title: @song_title,
              notes_default_song_page: @page_number
              } %>

<%= render 'scoreview/show' %>
<%= render 'form' %>
